{
  "name": "esbuild",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "esbuild",
    "build": "NODE_ENV=production esbuild ./main.js > ./output.js",
    "preview": "esbuild preview"
  },
  "devDependencies": {
    "esbuild": "0.20.2"
  }
}
