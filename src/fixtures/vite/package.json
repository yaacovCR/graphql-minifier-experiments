{
  "name": "vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "NODE_ENV=production vite build",
    "preview": "cd dist && http-server -o"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  },
  "dependencies": {
    "@rollup/plugin-replace": "^5.0.5",
    "http-server": "^14.1.1"
  }
}
